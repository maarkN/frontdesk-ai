; modules.conf — substitui o default da distro por um motivo só: o driver
; realtime precisa estar de pé ANTES dos consumidores de config (res_pjsip lê
; sorcery/extconfig no load; sem preload a ordem de autoload não é garantida).

[modules]
autoload = yes
preload = res_config_pgsql.so
